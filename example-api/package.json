{
	"name": "file-manager-api",
	"version": "1.0.0",
	"description": "RESTful File Manager API with PostgreSQL sync",
	"main": "src/index.ts",
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun --watch src/index.ts",
		"migrate": "bun run src/migrations/migrate.ts"
	},
	"dependencies": {
		"hono": "^3.12.0",
		"postgres": "^3.4.0",
		"prom-client": "^15.1.0",
		"tslog": "^4.9.3"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/pg": "^8.15.5"
	}
}
